# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /ping                 controllers.Application.ping()
POST        /state/random         controllers.Application.changeToRandomState(value: Int ?= 30)
POST        /state/:newState      controllers.Application.changeStateTo(newState: Boolean)
